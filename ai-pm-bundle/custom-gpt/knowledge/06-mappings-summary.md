# Mappings Summary

The mapping layer decides:
- which artifacts are required for each activity,
- which skill is the default transform,
- which checks must pass before an activity is considered ready.
